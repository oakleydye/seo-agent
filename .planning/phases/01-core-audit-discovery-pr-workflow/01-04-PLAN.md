---
phase: 01-core-audit-discovery-pr-workflow
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/fetcher/pagespeed.ts
  - src/fetcher/pagespeed.test.ts
  - src/executor/pr-formatter.ts
  - src/executor/github.ts
  - src/executor/pr-formatter.test.ts
  - src/executor/github.test.ts
autonomous: true
requirements:
  - AUDIT-04
  - AUDIT-05
  - AUDIT-10
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "PageSpeed fetcher calls https://www.googleapis.com/pagespeedonline/v5/runPagespeed and returns CoreWebVitals"
    - "PageSpeed fetcher handles API errors and quota exhaustion gracefully (returns null, does not throw)"
    - "PR formatter generates a Markdown PR body with executive summary (pass/fail counts, critical issues) followed by findings table"
    - "PR formatter includes Core Web Vitals section when vitals data is present"
    - "PR formatter escapes special characters in issue descriptions to prevent broken Markdown tables"
    - "GitHub executor creates branch named seo-audit/YYYY-MM-DD, creates empty commit, opens PR — returns PR URL"
    - "GitHub executor checks for existing open PR with same branch before creating a new one (idempotency)"
    - "Broken internal links (404s) and redirect chains are included in the PR findings from PageData"
  artifacts:
    - path: "src/fetcher/pagespeed.ts"
      provides: "fetchCoreWebVitals() that calls PageSpeed Insights API v5"
      exports:
        - fetchCoreWebVitals
    - path: "src/executor/pr-formatter.ts"
      provides: "formatPRBody() producing Markdown PR description"
      exports:
        - formatPRBody
        - formatPRTitle
    - path: "src/executor/github.ts"
      provides: "createAuditPR() using Octokit to create branch and open PR"
      exports:
        - createAuditPR
  key_links:
    - from: "src/fetcher/pagespeed.ts"
      to: "https://www.googleapis.com/pagespeedonline/v5/runPagespeed"
      via: "node-fetch GET request with apiKey query param"
    - from: "src/executor/github.ts"
      to: "@octokit/rest"
      via: "new Octokit({ auth: process.env.GITHUB_TOKEN })"
    - from: "src/executor/pr-formatter.ts"
      to: "src/types/index.ts"
      via: "imports Issue, AuditFindings, CoreWebVitals"
---

<objective>
Implement the PageSpeed Insights API fetcher (Core Web Vitals), the PR body formatter, and the GitHub PR executor. These three components complete the audit pipeline: fetcher retrieves performance data, formatter turns Issues + CoreWebVitals into readable Markdown, and the GitHub executor creates the branch and PR. Together they produce the deliverable audit report in the client's GitHub repo.

Purpose: This is the final output layer — without it, all audit data sits in memory and never reaches the client. The formatter's quality determines whether clients can act on findings.
Output: pagespeed.ts, pr-formatter.ts, github.ts, full test suite for each.
</objective>

<execution_context>
@/Users/oakleydye/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oakleydye/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-audit-discovery-pr-workflow/01-CONTEXT.md
@.planning/phases/01-core-audit-discovery-pr-workflow/01-RESEARCH.md
@.planning/phases/01-core-audit-discovery-pr-workflow/01-01-SUMMARY.md
@.planning/phases/01-core-audit-discovery-pr-workflow/01-02-SUMMARY.md
@.planning/phases/01-core-audit-discovery-pr-workflow/01-03-SUMMARY.md

<interfaces>
<!-- From src/types/index.ts (Plan 01) -->

```typescript
export interface CoreWebVitals {
  lcp: number | null;       // Largest Contentful Paint (ms)
  fid: number | null;       // First Input Delay (ms)
  cls: number | null;       // Cumulative Layout Shift (unitless)
  fcp: number | null;       // First Contentful Paint (ms)
  ttfb: number | null;      // Time to First Byte (ms)
  performanceScore: number | null; // 0-100
  source: 'pagespeed-insights';
}

export interface AuditFindings {
  siteId: string;
  auditDate: string;        // ISO date string YYYY-MM-DD
  pagesAudited: number;
  issues: Issue[];
  coreWebVitals?: CoreWebVitals;
}

export interface Issue {
  id: string;
  rule: string;
  severity: 'critical' | 'warning' | 'info';
  pageUrl: string;
  description: string;
  details?: string;
}
```

<!-- PR Structure (locked in CONTEXT.md) -->
PR format:
- Branch: `seo-audit/YYYY-MM-DD`
- One PR per site per audit run
- No files committed — findings in PR body/description only
- PR body: executive summary (pass/fail counts, critical issues) then detailed findings table
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PageSpeed Insights API fetcher and PR body formatter</name>
  <read_first>
    - /Users/oakleydye/code/seo-agent/src/types/index.ts (CoreWebVitals, AuditFindings, Issue interfaces)
    - /Users/oakleydye/code/seo-agent/.planning/phases/01-core-audit-discovery-pr-workflow/01-CONTEXT.md (PR body format requirements)
    - /Users/oakleydye/code/seo-agent/.planning/phases/01-core-audit-discovery-pr-workflow/01-RESEARCH.md (Pitfall 1: API Quota Exhaustion, Pitfall 4: Malformed PR)
  </read_first>
  <files>
    src/fetcher/pagespeed.ts, src/fetcher/pagespeed.test.ts,
    src/executor/pr-formatter.ts, src/executor/pr-formatter.test.ts
  </files>
  <behavior>
    - Test 1: fetchCoreWebVitals() calls URL matching `googleapis.com/pagespeedonline/v5/runPagespeed`
    - Test 2: fetchCoreWebVitals() returns CoreWebVitals with numeric lcp, cls, fcp, ttfb, performanceScore from mocked API response
    - Test 3: fetchCoreWebVitals() returns null when API responds with HTTP 429 (quota exhausted) — does NOT throw
    - Test 4: fetchCoreWebVitals() returns null when API responds with HTTP 403 (auth error) — does NOT throw
    - Test 5: formatPRTitle() returns string starting with 'SEO Audit:' containing the siteId
    - Test 6: formatPRBody() returns Markdown string containing '## Executive Summary'
    - Test 7: formatPRBody() returns string containing issue count for critical, warning, info levels
    - Test 8: formatPRBody() returns string containing a Markdown table with columns: Page URL, Issue, Severity, Description
    - Test 9: formatPRBody() includes '## Core Web Vitals' section when coreWebVitals is provided
    - Test 10: formatPRBody() omits Core Web Vitals section when coreWebVitals is undefined
    - Test 11: formatPRBody() escapes pipe characters `|` in issue descriptions (prevents broken table)
  </behavior>
  <action>
Create `src/fetcher/pagespeed.ts`:

```typescript
import fetch from 'node-fetch';
import { logger } from '../utils/logger.js';
import type { CoreWebVitals } from '../types/index.js';

const PAGESPEED_API_BASE = 'https://www.googleapis.com/pagespeedonline/v5/runPagespeed';

// Retry config for quota/rate limiting
const MAX_RETRIES = 3;
const BASE_DELAY_MS = 1000;

function sleep(ms: number): Promise<void> {
  return new Promise(resolve => setTimeout(resolve, ms));
}

/**
 * Fetch Core Web Vitals from PageSpeed Insights API v5.
 * Returns null on quota exhaustion, auth errors, or persistent failures.
 * Uses exponential backoff on 429 responses.
 */
export async function fetchCoreWebVitals(
  url: string,
  apiKey: string = process.env['GOOGLE_PAGESPEED_API_KEY'] ?? '',
): Promise<CoreWebVitals | null> {
  if (!apiKey) {
    logger.warn({ url }, 'GOOGLE_PAGESPEED_API_KEY not set — skipping PageSpeed fetch');
    return null;
  }

  const apiUrl = `${PAGESPEED_API_BASE}?url=${encodeURIComponent(url)}&key=${apiKey}&strategy=mobile&category=performance`;

  for (let attempt = 0; attempt < MAX_RETRIES; attempt++) {
    let response;
    try {
      response = await fetch(apiUrl);
    } catch (err) {
      logger.warn({ url, attempt, error: (err as Error).message }, 'PageSpeed fetch network error');
      if (attempt < MAX_RETRIES - 1) {
        await sleep(BASE_DELAY_MS * Math.pow(2, attempt));
        continue;
      }
      return null;
    }

    if (response.status === 429) {
      const delay = BASE_DELAY_MS * Math.pow(2, attempt);
      logger.warn({ url, attempt, delayMs: delay }, 'PageSpeed quota rate-limited, backing off');
      await sleep(delay);
      continue;
    }

    if (response.status === 403) {
      logger.error({ url, status: 403 }, 'PageSpeed API auth error — check GOOGLE_PAGESPEED_API_KEY');
      return null;
    }

    if (!response.ok) {
      logger.warn({ url, status: response.status }, 'PageSpeed API returned non-200');
      return null;
    }

    let data: unknown;
    try {
      data = await response.json();
    } catch {
      logger.warn({ url }, 'PageSpeed API response is not valid JSON');
      return null;
    }

    return parsePageSpeedResponse(data);
  }

  logger.warn({ url }, 'PageSpeed fetch exhausted retries');
  return null;
}

/**
 * Parse the PageSpeed Insights API v5 response into CoreWebVitals.
 * Handles missing fields gracefully (returns null for each missing metric).
 */
function parsePageSpeedResponse(data: unknown): CoreWebVitals | null {
  if (typeof data !== 'object' || data === null) return null;

  const d = data as Record<string, unknown>;
  const categories = d['lighthouseResult'] as Record<string, unknown> | undefined;
  const audits = (d['lighthouseResult'] as Record<string, unknown> | undefined)?.['audits'] as Record<string, unknown> | undefined;

  function getMetricValue(auditId: string): number | null {
    const audit = audits?.[auditId] as Record<string, unknown> | undefined;
    const val = audit?.['numericValue'];
    return typeof val === 'number' ? Math.round(val) : null;
  }

  function getFloatMetric(auditId: string): number | null {
    const audit = audits?.[auditId] as Record<string, unknown> | undefined;
    const val = audit?.['numericValue'];
    return typeof val === 'number' ? Math.round(val * 1000) / 1000 : null;
  }

  const perfCategory = (categories?.['categories'] as Record<string, unknown> | undefined)?.['performance'] as Record<string, unknown> | undefined;
  const score = typeof perfCategory?.['score'] === 'number'
    ? Math.round((perfCategory['score'] as number) * 100)
    : null;

  return {
    lcp: getMetricValue('largest-contentful-paint'),
    fid: getMetricValue('max-potential-fid'),
    cls: getFloatMetric('cumulative-layout-shift'),
    fcp: getMetricValue('first-contentful-paint'),
    ttfb: getMetricValue('server-response-time'),
    performanceScore: score,
    source: 'pagespeed-insights',
  };
}
```

Create `src/fetcher/pagespeed.test.ts`:

```typescript
import { describe, it, expect, vi } from 'vitest';
import { fetchCoreWebVitals } from './pagespeed.js';

vi.mock('node-fetch', () => ({
  default: vi.fn(),
}));

import fetch from 'node-fetch';
const mockFetch = fetch as unknown as ReturnType<typeof vi.fn>;

function makeApiResponse(data: unknown) {
  return Promise.resolve({
    ok: true,
    status: 200,
    json: () => Promise.resolve(data),
  });
}

const mockPSIResponse = {
  lighthouseResult: {
    categories: {
      performance: { score: 0.72 },
    },
    audits: {
      'largest-contentful-paint': { numericValue: 2800 },
      'max-potential-fid': { numericValue: 120 },
      'cumulative-layout-shift': { numericValue: 0.08 },
      'first-contentful-paint': { numericValue: 1500 },
      'server-response-time': { numericValue: 350 },
    },
  },
};

describe('fetchCoreWebVitals', () => {
  it('calls PageSpeed Insights API URL', async () => {
    mockFetch.mockImplementation(() => makeApiResponse(mockPSIResponse));
    await fetchCoreWebVitals('https://example.com', 'test-key');
    const calledUrl = mockFetch.mock.calls[0]![0] as string;
    expect(calledUrl).toContain('googleapis.com/pagespeedonline/v5/runPagespeed');
    expect(calledUrl).toContain(encodeURIComponent('https://example.com'));
  });

  it('returns CoreWebVitals with parsed metrics', async () => {
    mockFetch.mockImplementation(() => makeApiResponse(mockPSIResponse));
    const vitals = await fetchCoreWebVitals('https://example.com', 'test-key');
    expect(vitals).not.toBeNull();
    expect(vitals!.lcp).toBe(2800);
    expect(vitals!.performanceScore).toBe(72);
    expect(vitals!.source).toBe('pagespeed-insights');
  });

  it('returns null on 429 quota error without throwing', async () => {
    mockFetch.mockResolvedValue({ ok: false, status: 429 });
    const vitals = await fetchCoreWebVitals('https://example.com', 'test-key');
    expect(vitals).toBeNull();
  });

  it('returns null on 403 auth error without throwing', async () => {
    mockFetch.mockResolvedValue({ ok: false, status: 403 });
    const vitals = await fetchCoreWebVitals('https://example.com', 'test-key');
    expect(vitals).toBeNull();
  });

  it('returns null when API key is not set', async () => {
    const vitals = await fetchCoreWebVitals('https://example.com', '');
    expect(vitals).toBeNull();
  });
});
```

Create `src/executor/pr-formatter.ts`:

```typescript
import type { AuditFindings, Issue, CoreWebVitals } from '../types/index.js';

/**
 * Escape pipe characters and newlines in Markdown table cells.
 */
function escapeTableCell(value: string): string {
  return value.replace(/\|/g, '\\|').replace(/\n/g, ' ').replace(/\r/g, '');
}

function severityEmoji(severity: Issue['severity']): string {
  switch (severity) {
    case 'critical': return '🔴 Critical';
    case 'warning': return '🟡 Warning';
    case 'info': return '🔵 Info';
  }
}

/**
 * Format the PR title for an audit run.
 * Example: "SEO Audit: acme-website — 2024-01-15 (3 critical issues)"
 */
export function formatPRTitle(findings: AuditFindings): string {
  const criticalCount = findings.issues.filter(i => i.severity === 'critical').length;
  const suffix = criticalCount > 0 ? ` (${criticalCount} critical issue${criticalCount === 1 ? '' : 's'})` : '';
  return `SEO Audit: ${findings.siteId} — ${findings.auditDate}${suffix}`;
}

/**
 * Format the full PR body as GitHub-flavored Markdown.
 * Structure:
 *   ## Executive Summary
 *   | metric | value |
 *   ## Critical Issues (if any)
 *   ## All Findings
 *   | Page URL | Issue | Severity | Description |
 *   ## Core Web Vitals (if available)
 */
export function formatPRBody(findings: AuditFindings): string {
  const { siteId, auditDate, pagesAudited, issues, coreWebVitals } = findings;

  const criticalCount = issues.filter(i => i.severity === 'critical').length;
  const warningCount = issues.filter(i => i.severity === 'warning').length;
  const infoCount = issues.filter(i => i.severity === 'info').length;
  const totalCount = issues.length;

  const statusBadge = criticalCount > 0 ? '**Status: NEEDS ATTENTION**' : '**Status: PASS** (no critical issues)';

  const sections: string[] = [];

  // ─── Executive Summary ───────────────────────────────────────────────────
  sections.push(`## Executive Summary

${statusBadge}

| Metric | Value |
|--------|-------|
| Site | \`${siteId}\` |
| Audit Date | ${auditDate} |
| Pages Audited | ${pagesAudited} |
| Total Issues | ${totalCount} |
| 🔴 Critical | ${criticalCount} |
| 🟡 Warning | ${warningCount} |
| 🔵 Info | ${infoCount} |`);

  // ─── Critical Issues (if any) ────────────────────────────────────────────
  const criticalIssues = issues.filter(i => i.severity === 'critical');
  if (criticalIssues.length > 0) {
    const rows = criticalIssues.map(issue => {
      const pageDisplay = escapeTableCell(issue.pageUrl);
      const ruleDisplay = escapeTableCell(issue.rule);
      const descDisplay = escapeTableCell(issue.description);
      return `| ${pageDisplay} | ${ruleDisplay} | ${descDisplay} |`;
    }).join('\n');

    sections.push(`## 🔴 Critical Issues

| Page URL | Rule | Description |
|----------|------|-------------|
${rows}`);
  }

  // ─── All Findings ────────────────────────────────────────────────────────
  if (issues.length === 0) {
    sections.push(`## All Findings

No issues detected. Great work!`);
  } else {
    // Sort: critical first, then warning, then info
    const sorted = [...issues].sort((a, b) => {
      const order = { critical: 0, warning: 1, info: 2 } as const;
      return order[a.severity] - order[b.severity];
    });

    const rows = sorted.map(issue => {
      const pageDisplay = escapeTableCell(issue.pageUrl);
      const ruleDisplay = escapeTableCell(issue.rule);
      const sevDisplay = severityEmoji(issue.severity);
      const descDisplay = escapeTableCell(issue.description);
      return `| ${pageDisplay} | ${ruleDisplay} | ${sevDisplay} | ${descDisplay} |`;
    }).join('\n');

    sections.push(`## All Findings

| Page URL | Rule | Severity | Description |
|----------|------|----------|-------------|
${rows}`);
  }

  // ─── Core Web Vitals ─────────────────────────────────────────────────────
  if (coreWebVitals) {
    sections.push(formatCoreWebVitalsSection(coreWebVitals));
  }

  // ─── Footer ──────────────────────────────────────────────────────────────
  sections.push(`---
*Generated by SEO Agent on ${auditDate}. Review findings and merge when addressed.*`);

  return sections.join('\n\n');
}

function formatCoreWebVitalsSection(vitals: CoreWebVitals): string {
  function fmt(val: number | null, unit: string = 'ms'): string {
    return val !== null ? `${val}${unit}` : 'N/A';
  }

  const scoreLabel = vitals.performanceScore !== null
    ? vitals.performanceScore >= 90 ? '🟢 Good'
      : vitals.performanceScore >= 50 ? '🟡 Needs Improvement'
      : '🔴 Poor'
    : 'N/A';

  return `## Core Web Vitals

| Metric | Value | Source |
|--------|-------|--------|
| Performance Score | ${scoreLabel} (${vitals.performanceScore ?? 'N/A'}/100) | PageSpeed Insights |
| LCP (Largest Contentful Paint) | ${fmt(vitals.lcp)} | PageSpeed Insights |
| FID (First Input Delay) | ${fmt(vitals.fid)} | PageSpeed Insights |
| CLS (Cumulative Layout Shift) | ${fmt(vitals.cls, '')} | PageSpeed Insights |
| FCP (First Contentful Paint) | ${fmt(vitals.fcp)} | PageSpeed Insights |
| TTFB (Time to First Byte) | ${fmt(vitals.ttfb)} | PageSpeed Insights |`;
}
```

Create `src/executor/pr-formatter.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { formatPRTitle, formatPRBody } from './pr-formatter.js';
import type { AuditFindings } from '../types/index.js';

const baseFindings: AuditFindings = {
  siteId: 'test-site',
  auditDate: '2024-01-15',
  pagesAudited: 10,
  issues: [],
};

const findingsWithIssues: AuditFindings = {
  ...baseFindings,
  issues: [
    { id: 'AUDIT-01', rule: 'missing-title-tag', severity: 'critical', pageUrl: 'https://a.com/', description: 'Missing title' },
    { id: 'AUDIT-02', rule: 'missing-meta-description', severity: 'warning', pageUrl: 'https://a.com/about', description: 'Missing description' },
    { id: 'AUDIT-07', rule: 'missing-canonical', severity: 'info', pageUrl: 'https://a.com/blog', description: 'No canonical' },
  ],
};

describe('formatPRTitle', () => {
  it('starts with "SEO Audit:"', () => {
    expect(formatPRTitle(baseFindings)).toMatch(/^SEO Audit:/);
  });

  it('includes siteId in title', () => {
    expect(formatPRTitle(baseFindings)).toContain('test-site');
  });

  it('includes critical count when there are critical issues', () => {
    expect(formatPRTitle(findingsWithIssues)).toContain('1 critical issue');
  });

  it('does not include critical count when no critical issues', () => {
    expect(formatPRTitle(baseFindings)).not.toContain('critical');
  });
});

describe('formatPRBody', () => {
  it('contains Executive Summary section', () => {
    expect(formatPRBody(baseFindings)).toContain('## Executive Summary');
  });

  it('contains issue counts', () => {
    const body = formatPRBody(findingsWithIssues);
    expect(body).toContain('| 🔴 Critical | 1 |');
    expect(body).toContain('| 🟡 Warning | 1 |');
    expect(body).toContain('| 🔵 Info | 1 |');
  });

  it('contains All Findings table with column headers', () => {
    const body = formatPRBody(findingsWithIssues);
    expect(body).toContain('| Page URL | Rule | Severity | Description |');
  });

  it('includes Core Web Vitals section when vitals provided', () => {
    const findings: AuditFindings = {
      ...baseFindings,
      coreWebVitals: { lcp: 2500, fid: 100, cls: 0.05, fcp: 1200, ttfb: 300, performanceScore: 85, source: 'pagespeed-insights' },
    };
    expect(formatPRBody(findings)).toContain('## Core Web Vitals');
    expect(formatPRBody(findings)).toContain('2500ms');
  });

  it('omits Core Web Vitals section when vitals not provided', () => {
    expect(formatPRBody(baseFindings)).not.toContain('## Core Web Vitals');
  });

  it('escapes pipe characters in issue descriptions to prevent broken tables', () => {
    const findingsWithPipe: AuditFindings = {
      ...baseFindings,
      issues: [{
        id: 'AUDIT-01', rule: 'test', severity: 'info', pageUrl: 'https://a.com/',
        description: 'Issue with | pipe character in description',
      }],
    };
    const body = formatPRBody(findingsWithPipe);
    expect(body).toContain('\\|');
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/oakleydye/code/seo-agent && npx tsc --noEmit && npm test -- --reporter=verbose 2>&1 | grep -E "pagespeed|formatter|PASS|FAIL|Tests"</automated>
  </verify>
  <acceptance_criteria>
    - src/fetcher/pagespeed.ts exports `fetchCoreWebVitals(url, apiKey): Promise<CoreWebVitals | null>`
    - src/fetcher/pagespeed.ts contains `googleapis.com/pagespeedonline/v5/runPagespeed` URL (grep confirms)
    - fetchCoreWebVitals returns null on 429 and 403 — does not throw (tested in pagespeed.test.ts)
    - src/executor/pr-formatter.ts exports `formatPRTitle` and `formatPRBody`
    - formatPRBody output contains '## Executive Summary' (grep test confirms)
    - formatPRBody output contains escaped `\|` for pipe characters in issue descriptions (test confirms)
    - `npm test` exits 0 (all pagespeed and pr-formatter tests pass)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PageSpeed fetcher with quota-safe retry logic and PR formatter with pipe-escaped Markdown table implemented — all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GitHub PR executor</name>
  <read_first>
    - /Users/oakleydye/code/seo-agent/src/types/index.ts (AuditFindings, AuditRunState)
    - /Users/oakleydye/code/seo-agent/src/executor/pr-formatter.ts (formatPRTitle, formatPRBody — must exist from Task 1)
    - /Users/oakleydye/code/seo-agent/.planning/phases/01-core-audit-discovery-pr-workflow/01-CONTEXT.md (branch naming: seo-audit/YYYY-MM-DD, PR structure decisions)
    - /Users/oakleydye/code/seo-agent/.planning/phases/01-core-audit-discovery-pr-workflow/01-RESEARCH.md (Pitfall 3: Secrets Leakage, Pitfall 5: Configuration)
  </read_first>
  <files>src/executor/github.ts, src/executor/github.test.ts</files>
  <behavior>
    - Test 1: createAuditPR() calls Octokit repos.getBranch to check if branch exists before creating
    - Test 2: createAuditPR() calls Octokit git.createRef with branch name `seo-audit/YYYY-MM-DD`
    - Test 3: createAuditPR() calls Octokit pulls.create with title from formatPRTitle() and body from formatPRBody()
    - Test 4: createAuditPR() returns PR URL (html_url from Octokit response)
    - Test 5: createAuditPR() does NOT create a new branch if one already exists with same name (idempotency)
    - Test 6: createAuditPR() does NOT create a new PR if an open PR already exists for the branch
    - Test 7: createAuditPR() throws Error with message containing 'GITHUB_TOKEN' when env var not set
  </behavior>
  <action>
Create `src/executor/github.ts`:

```typescript
import { Octokit } from '@octokit/rest';
import { logger } from '../utils/logger.js';
import { formatPRTitle, formatPRBody } from './pr-formatter.js';
import type { AuditFindings } from '../types/index.js';

export interface CreatePROptions {
  gitHubRepo: string;  // "owner/repo" format
  findings: AuditFindings;
  githubToken?: string;
}

export interface PRResult {
  prUrl: string;
  prNumber: number;
  wasExisting: boolean;
}

/**
 * Create (or find existing) audit PR for the given findings.
 * Branch naming: seo-audit/YYYY-MM-DD
 * Idempotent: returns existing PR if branch/PR already exists for today.
 */
export async function createAuditPR(options: CreatePROptions): Promise<PRResult> {
  const { gitHubRepo, findings } = options;
  const token = options.githubToken ?? process.env['GITHUB_TOKEN'];

  if (!token) {
    throw new Error('GITHUB_TOKEN environment variable is required for PR creation');
  }

  const [owner, repo] = gitHubRepo.split('/');
  if (!owner || !repo) {
    throw new Error(`Invalid gitHubRepo format: "${gitHubRepo}" — expected "owner/repo"`);
  }

  const octokit = new Octokit({ auth: token });
  const branchName = `seo-audit/${findings.auditDate}`;
  const title = formatPRTitle(findings);
  const body = formatPRBody(findings);

  // ─── Check for existing PR ───────────────────────────────────────────────
  const existingPRs = await octokit.pulls.list({
    owner,
    repo,
    state: 'open',
    head: `${owner}:${branchName}`,
    per_page: 1,
  });

  if (existingPRs.data.length > 0) {
    const existing = existingPRs.data[0]!;
    logger.info({ branchName, prUrl: existing.html_url }, 'Existing PR found — skipping creation');
    return {
      prUrl: existing.html_url,
      prNumber: existing.number,
      wasExisting: true,
    };
  }

  // ─── Get default branch SHA (needed to create new branch) ────────────────
  const repoData = await octokit.repos.get({ owner, repo });
  const defaultBranch = repoData.data.default_branch;

  const defaultBranchData = await octokit.repos.getBranch({
    owner,
    repo,
    branch: defaultBranch,
  });
  const baseSha = defaultBranchData.data.commit.sha;

  // ─── Create branch if it doesn't exist ───────────────────────────────────
  let branchExists = false;
  try {
    await octokit.repos.getBranch({ owner, repo, branch: branchName });
    branchExists = true;
    logger.info({ branchName }, 'Branch already exists, skipping creation');
  } catch (err: unknown) {
    const octokitErr = err as { status?: number };
    if (octokitErr?.status !== 404) throw err;
    // Branch does not exist — create it
  }

  if (!branchExists) {
    await octokit.git.createRef({
      owner,
      repo,
      ref: `refs/heads/${branchName}`,
      sha: baseSha,
    });
    logger.info({ branchName }, 'Branch created');
  }

  // ─── Create PR ────────────────────────────────────────────────────────────
  const prResponse = await octokit.pulls.create({
    owner,
    repo,
    title,
    body,
    head: branchName,
    base: defaultBranch,
  });

  logger.info({ prUrl: prResponse.data.html_url, prNumber: prResponse.data.number }, 'PR created');

  return {
    prUrl: prResponse.data.html_url,
    prNumber: prResponse.data.number,
    wasExisting: false,
  };
}
```

Create `src/executor/github.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { createAuditPR } from './github.js';
import type { AuditFindings } from '../types/index.js';

// Mock Octokit
vi.mock('@octokit/rest', () => ({
  Octokit: vi.fn().mockImplementation(() => mockOctokit),
}));

const mockOctokit = {
  pulls: {
    list: vi.fn(),
    create: vi.fn(),
  },
  repos: {
    get: vi.fn(),
    getBranch: vi.fn(),
  },
  git: {
    createRef: vi.fn(),
  },
};

const baseFindings: AuditFindings = {
  siteId: 'test-site',
  auditDate: '2024-01-15',
  pagesAudited: 5,
  issues: [],
};

beforeEach(() => {
  vi.clearAllMocks();
  mockOctokit.pulls.list.mockResolvedValue({ data: [] });
  mockOctokit.repos.get.mockResolvedValue({ data: { default_branch: 'main' } });
  mockOctokit.repos.getBranch.mockRejectedValueOnce({ status: 404 }); // branch doesn't exist
  mockOctokit.git.createRef.mockResolvedValue({});
  mockOctokit.pulls.create.mockResolvedValue({
    data: { html_url: 'https://github.com/owner/repo/pull/1', number: 1 },
  });
});

describe('createAuditPR', () => {
  it('throws when GITHUB_TOKEN is not provided', async () => {
    await expect(
      createAuditPR({ gitHubRepo: 'owner/repo', findings: baseFindings, githubToken: '' }),
    ).rejects.toThrow(/GITHUB_TOKEN/);
  });

  it('creates branch with seo-audit/YYYY-MM-DD naming', async () => {
    await createAuditPR({ gitHubRepo: 'owner/repo', findings: baseFindings, githubToken: 'tok' });
    expect(mockOctokit.git.createRef).toHaveBeenCalledWith(
      expect.objectContaining({ ref: 'refs/heads/seo-audit/2024-01-15' }),
    );
  });

  it('creates PR and returns URL', async () => {
    const result = await createAuditPR({ gitHubRepo: 'owner/repo', findings: baseFindings, githubToken: 'tok' });
    expect(result.prUrl).toBe('https://github.com/owner/repo/pull/1');
    expect(result.wasExisting).toBe(false);
  });

  it('returns existing PR without creating new one when open PR exists', async () => {
    mockOctokit.pulls.list.mockResolvedValue({
      data: [{ html_url: 'https://github.com/owner/repo/pull/99', number: 99 }],
    });

    const result = await createAuditPR({ gitHubRepo: 'owner/repo', findings: baseFindings, githubToken: 'tok' });
    expect(result.prUrl).toBe('https://github.com/owner/repo/pull/99');
    expect(result.wasExisting).toBe(true);
    expect(mockOctokit.pulls.create).not.toHaveBeenCalled();
    expect(mockOctokit.git.createRef).not.toHaveBeenCalled();
  });

  it('skips branch creation if branch already exists', async () => {
    mockOctokit.repos.getBranch
      .mockResolvedValueOnce({ data: { commit: { sha: 'abc123' } } }) // default branch
      .mockResolvedValueOnce({ data: {} }); // audit branch exists

    await createAuditPR({ gitHubRepo: 'owner/repo', findings: baseFindings, githubToken: 'tok' });
    expect(mockOctokit.git.createRef).not.toHaveBeenCalled();
  });

  it('PR title includes siteId from formatPRTitle', async () => {
    await createAuditPR({ gitHubRepo: 'owner/repo', findings: baseFindings, githubToken: 'tok' });
    const callArgs = mockOctokit.pulls.create.mock.calls[0]![0] as { title: string };
    expect(callArgs.title).toContain('test-site');
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/oakleydye/code/seo-agent && npx tsc --noEmit && npm test -- --reporter=verbose 2>&1 | grep -E "github|PR|PASS|FAIL|Tests"</automated>
  </verify>
  <acceptance_criteria>
    - src/executor/github.ts exports `createAuditPR` function
    - createAuditPR uses branch name pattern `seo-audit/${findings.auditDate}` (grep: `seo-audit/` in src/executor/github.ts)
    - createAuditPR checks for existing open PRs before creating (grep: `pulls.list` in src/executor/github.ts)
    - createAuditPR throws Error matching /GITHUB_TOKEN/ when token not provided
    - `npm test` exits 0 (all github executor tests pass)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GitHub PR executor creates idempotent audit PRs with seo-audit/YYYY-MM-DD branches — existing PR detection prevents duplicates, all tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/oakleydye/code/seo-agent
npx tsc --noEmit
npm test
grep -n "googleapis.com/pagespeedonline" src/fetcher/pagespeed.ts
grep -n "seo-audit/" src/executor/github.ts
grep -n "pulls.list" src/executor/github.ts
```
</verification>

<success_criteria>
1. `npx tsc --noEmit` exits 0
2. `npm test` exits 0 (all tests in pagespeed, pr-formatter, github pass)
3. `fetchCoreWebVitals('https://x.com', '')` returns null without throwing
4. `formatPRBody(findings)` output contains '## Executive Summary' and '## All Findings'
5. `createAuditPR()` without GITHUB_TOKEN throws error matching /GITHUB_TOKEN/
6. Pipe character `|` in issue description is escaped as `\|` in formatted PR body
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-audit-discovery-pr-workflow/01-04-SUMMARY.md`
</output>
